<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The renderer is written against ES 3.0; declaring it keeps the app off
         devices that could only fail at first frame. -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <!-- Stylus pressure and tilt are what the brushes are built around. -->
    <uses-feature android:name="android.hardware.touchscreen.pen" android:required="false" />

    <application
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.Anvil">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
